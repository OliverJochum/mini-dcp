name: Test
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v2
     - name: Install & Build
       run: ./gradlew clean build
     - name: Run tests
       run: ./gradlew test
